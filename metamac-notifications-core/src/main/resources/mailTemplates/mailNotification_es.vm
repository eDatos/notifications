Instituto Canario de Estadística
Notificaciones de ISTACbase

-----------------------------------------------------

- Aplicación emisora: ${notification.sendingApplication}
- Fecha de creación: ${createdDate}
#if( "$!notification.sendingUser" != "" )
- Usuario emisor:  ${notification.sendingUser}
#end
- Urn de aviso: ${notification.urn}
- Tipo de aviso: ${notification.notificationType}

- Mensajes:
	#foreach( $item in ${notification.messages} )
	  -- $item.text
	#end